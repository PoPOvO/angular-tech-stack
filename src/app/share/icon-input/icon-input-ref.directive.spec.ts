import { IconInputRefDirective } from './icon-input-ref.directive';

describe('IconInputRefDirective', () => {
  it('should create an instance', () => {
    const directive = new IconInputRefDirective();
    expect(directive).toBeTruthy();
  });
});
